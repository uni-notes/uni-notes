#include <stdio.h>

int main()
{
	int n;
	printf("Number: "); scanf("%d", &n);
	printf("\nThe divisors of %d are\n", n);

	int i = 1;
	while(i<=n)
	{
		if(n%i==0)
			printf("%d\n", i);
		i++;
	}

	return 0;
}
